h2
  - Currency.all.order(:title).each do |c|
    - next if c.title == 'USD'
    - if c.id == @currency.id
      = @currency.title
    - else
      a<> href=path_to(:exrate).with(c.id) = c.title
  a href=path_to(:exrate_new) Create new

== slim :currency_form, locals: {c: @currency}

table.savings-table
  tr
    th id
    th actual_date
    th date
    th rate
    th record_type
  - @prices.each do |p|
    tr
      td = p.id
      td = p.actual_date
      td = p.date
      td = p.rate
      td = p.record_type
